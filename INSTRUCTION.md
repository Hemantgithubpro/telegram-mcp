
1. Create session id, api keys, from telegram
2. uv sync
3. create `opencode.json`
4. run main.py with `MCP_TRANSPORT=sse uv run main.py`
5. opencode mcp add

verify with `opencode mcp list` to ensure the MCP has been added successfully.
then chat with opencode

